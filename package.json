{
  "name": "resolve-works",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.3.0",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "generate-related": "node tools/generate-related.mjs && vite build",
    "generate-embeddings": "node tools/generate-embeddings.mjs && vite build",
    "generate-index": "node tools/generate-index.mjs",
    "generate-markdown": "node tools/generate-markdown.mjs && vite build",
    "generate-og": "node tools/generate-og.mjs && vite build",
    "generate": "pnpm generate-index && pnpm generate-related && pnpm generate-embeddings && pnpm generate-markdown && pnpm generate-og",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@huggingface/transformers": "^4.2.0",
    "@resvg/resvg-js": "^2.6.2",
    "@sveltejs/adapter-static": "^3.0.10",
    "@sveltejs/kit": "^2.70.0",
    "@sveltejs/vite-plugin-svelte": "^7.2.0",
    "mdsvex": "^0.12.7",
    "node-html-parser": "^9.0.0",
    "svelte": "^5.56.6",
    "umap-js": "^1.4.0",
    "vite": "^8.1.5"
  },
  "dependencies": {
    "d3": "^7.9.0"
  }
}
